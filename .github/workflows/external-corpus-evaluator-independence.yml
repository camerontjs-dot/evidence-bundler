name: External corpus evaluator independence

on:
  pull_request:
    paths:
      - "research/external_corpus_evaluator_independence_v1/**"
      - "tests/test_external_corpus_evaluator_independence.py"
      - ".github/workflows/external-corpus-evaluator-independence.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  assurance:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install "pytest>=8,<10"
      - run: python -m pytest -q tests/test_external_corpus_evaluator_independence.py
