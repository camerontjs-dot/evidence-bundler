# Explicit alternative explanations

The execution is terminally `FALSIFIED` by information-acquisition exposure. Once that exposure occurred, later source diagnostics cannot resolve scientific alternatives cleanly.

| # | alternative | status in this execution | basis |
|---:|---|---|---|
| 1 | FreshStack aperture is secretly retrieval-conditioned | `PRE-EXPOSURE SOURCE-CODE EVIDENCE AGAINST; GATE NOT COMPLETED` | pinned loader code derives corpus/query from the same topic subset without a retrieval candidate list |
| 2 | FreshStack source-version reconstruction is ambiguous | `UNKNOWN` | post-exposure history diagnostics are not clean gate evidence |
| 3 | SciFact immutable reconstruction is not reproducible | `UNKNOWN IN CLEAN EXECUTION` | archive reproduction occurred only after the FreshStack exposure |
| 4 | Passage segmentation manufactures decisive evidence | `NOT_TESTED` | no clean passage materialization |
| 5 | Independent judges follow the same superficial cue | `NOT_TESTED` | no scientific adjudicators created |
| 6 | Published qrels leaked before gold freeze | `SUPPORTED — TERMINAL FALSIFIER` | query parquet iterator deserialized all columns; pinned loader establishes `nuggets` is the qrel-bearing field |
| 7 | Metadata leaks relevance | `NOT_TESTED` | no scientific adjudication |
| 8 | Evaluator contract admits multiple reasonable interpretations | `NOT_TESTED` | fresh independent evaluator B unavailable |
| 9 | Evaluator B was not genuinely independent | `SUPPORTED AS LIMITATION` | no independent evaluator B was created |
| 10 | Gold stability depends on one corpus-specific convention | `NOT_TESTED` | no gold created |
| 11 | Information-acquisition surfaces leaked retrieval/qrel behavior despite allow-list | `SUPPORTED — TERMINAL FALSIFIER` | output filtering prevented persistence/rendering but did not prevent forbidden qrel-bearing columns from entering process memory |
| 12 | Upstream source reconstruction silently substituted current HEAD | `NO EVIDENCE OF SUBSTITUTION BEFORE STOP` | the run left source binding unresolved rather than using current HEAD, but post-exposure diagnostics cannot establish a scientific gate |

## Strongest remaining alternative

There is no contract-consistent alternative that turns the acquisition into a clean execution. One could define “exposure” narrowly as only rendering a qrel value to the model/user, but the frozen task explicitly requires **mechanical information separation** and says that if the information-acquisition machinery violates the firewall, the failure must be preserved and execution stopped. Deserializing the forbidden field is therefore sufficient.

The later diagnostic observation that the pinned query revision appears inconsistent with recorded row counts may represent a second real methodological defect, but it must be independently re-established in a new fresh execution before it can be treated as scientific evidence.

## Cheapest successor discriminator

Before another Pilot 0A completion attempt, build and assure a source-access harness that enforces an allow-listed **column projection before deserialization**. It must prove that FreshStack query acquisition can read only identity/text fields while qrel-bearing `nuggets`, answer/evidence fields, candidate lists, and result fields never enter process memory. Then rerun the source gates in a genuinely fresh context. Do not repair Pilot 0A, create the 24–30-case apparatus, or run a retriever.
