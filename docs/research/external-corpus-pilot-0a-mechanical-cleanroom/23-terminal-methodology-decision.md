# Evidence Bundler External Corpus Methodology Pilot 0A — Mechanically Isolated Clean-Room Completion

## Terminal disposition

# `FALSIFIED`

The exact frozen ten-case Pilot 0A object cannot be reconstructed from its own preregistered FreshStack query revision. At `freshstack/queries-oct-2024-unfiltered@00150066ff2959688ad03ce7148ffb652f2fee38`, the frozen `angular` physical row 248 does not exist because the pinned partition has 230 rows, and the frozen `godot` row 36 does not exist because that exact revision contains no `godot` test data file.

Repair would require changing a frozen case identity, changing the pinned query revision, or substituting another dataset state. All are forbidden. The hard gate therefore stopped the scientific execution before passage materialization, adjudication, published-qrel comparison, or gold-stability testing.

This is a clean methodological falsification, not a contamination failure.

---

## OBSERVED

- Production `main` at execution start: `b9917f371d00a8dfaad20ab63e1daebc8c3c5f37`.
- Pilot 0A PR #20 is closed/unmerged; frozen branch head `357fe735067dbbd3d54f8872ffc8391dac724950`; preregistration commit `bf6a347704d8711628e044f46c0c3fb9fa4557df`.
- Evaluator-contract PR #21 is open/unmerged at `764144f3da77140a8e542158948b4e88d40a7421`; contract Git blob `61ad95dab08c89c34e3416c2a3b9b0f35dabb7f0`.
- PR #22 and #23 are closed/unmerged contamination records at `4d91e7a3de78981e4f73489aab179767c58c1914` and `a24542a32acb4f3c04da2dd5bfd8aeaa63123769` respectively.
- PR #17 is closed/unmerged at `27be0c85fdaae9e56a7622e007ca062575e9c433` with live `disposition:superseded` label; PR #18 is closed/unmerged at `f0331faa6f97b655de5b22dc419e21f3c0205df3` with live `disposition:falsified` label. Their result-bearing bodies were not opened.
- FreshStack construction code at `f1c4ec96477f5100f10c83798d33b3101db727fa` records clone `HEAD` as `commit_id`; its emitted source URL uses the repository default branch.
- The exact FreshStack corpus revision `069f66dc323e163b48b10d08408d282733d4393b` contains all five topic corpus partitions.
- The exact FreshStack query revision `00150066ff2959688ad03ce7148ffb652f2fee38` contains only four topic test files: `langchain`, `yolo`, `laravel`, `angular`; no `godot` test file is present.
- Observed query rows at that exact revision: langchain 318, yolo 94, laravel 310, angular 230, godot 0.
- Frozen selected query indices 271/42/121 reconstruct for langchain/yolo/laravel; angular 248 and godot 36 do not.
- FreshStack complete topic corpus partitions are definable without qrels or retrieval through the pinned `DataLoader` topic subset semantics.
- Same-dataset FreshStack corpus Git-history inspection found no checked published revision exposing a usable non-null construction `commit_id`; upstream source-byte binding was not executed.
- SciFact canonical archive reproduced exact preregistered SHA-256 `11c621288d41ac144d29b13b0f8503b3820b7d6e8b1f6ff24dff335c196d76be`, 3,115,079 bytes.
- All five frozen SciFact physical claim rows reconstructed and all five `cited_doc_ids` resolved.
- SciFact authoritative pinned license states CC BY 4.0 for claims/evidence, ODC-By 1.0 for abstracts via S2ORC, Apache-2.0 for code.
- MainFrame/Conduit failed with HTTP 404 before a separate evaluator context could be created.
- No scientific gold was created.

## INFERENCE

- Gate 1 is `FALSIFIED`: the exact frozen FreshStack scientific query object is internally inconsistent with its pinned dataset revision.
- Gate 2 aperture mechanics independently pass: complete topic partitions are `DETERMINISTICALLY DERIVED WITHOUT RETRIEVAL`; this does not rescue missing frozen query identities.
- Gate 3 is `PASS`: SciFact has a reproducible content-addressed archive identity despite its mutable canonical `release/latest` URL.
- Gate 4 is `INDEPENDENCE NOT ESTABLISHED`, not a successful independent cross-check and not a simulated one.
- The frozen Pilot 0A object cannot lawfully proceed to Gates 5–10 because changing the query revision or cases would constitute repair of the frozen object.
- The terminal result is caused by source/query identity reconstruction, not retrieval leakage or qrel leakage.

## HYPOTHESIS

- The query revision pinned in PR #20 may be an earlier or incorrect dataset revision relative to the row counts used at preregistration. A different historical revision may contain the intended `angular` and `godot` worlds.
- If so, the core methodological lesson is that future preregistration must mechanically verify selected row existence and hash the exact selected query rows before the object is sealed.
- FreshStack source-byte provenance may also require freezing an explicit commit-bearing historical corpus identity or a generated immutable source manifest at preregistration rather than attempting to recover it afterward.

These hypotheses do not rescue or modify Pilot 0A.

## UNKNOWN

- Which historical FreshStack query revision, if any, matches all five row counts recorded in PR #20.
- Whether a deterministic official-to-commit-bearing FreshStack corpus join exists outside the checked official dataset history.
- Exact historical upstream source bytes for all FreshStack corpus rows.
- FreshStack per-upstream-repository licensing for the unbound historical sources.
- Evaluator-contract independent reproducibility.
- Passage correspondence for the ten-case scientific object.
- Independent scientific relevance judgments and disagreement structure.
- Published-qrel divergence.
- Segmentation/gold stability.
- Full corpus-authoring influence after relevance adjudication.

---

## 1. Clean-room access status

`CLEAN WITH RECORDED NON-CONTAMINATING DEVIATION`.

A pre-execution allow-list/forbidden-surface receipt was committed before corpus work. Direct pinned dataset/Git artifacts and a source-only GitHub Actions acquisition harness were used. General web search was not used for FreshStack provenance. The Actions firewall reported no FreshStack README, qrel, candidate-list, retrieval-result, or retriever exposure.

An early overbroad GitHub PR-search call rendered non-result bodies for PR #20/#21/#22/#23 and an unrelated PR. It rendered no retrieval metric value or candidate list; it was recorded and the surface was excluded thereafter.

The named CAL Pipeline governance files could not be located through the available GitHub code-search surface before execution. The run therefore followed the exact frozen PR #20 preregistration and this stricter task contract; that lookup limitation is preserved rather than silently treated as satisfied.

## 2. FreshStack provenance

`FALSIFIED` at frozen query identity reconstruction.

The official corpus bytes themselves were acquired at the exact pinned corpus revision and hashed. However, two of five frozen query cases do not exist at the exact pinned query revision. Independent source-byte provenance also remained incomplete because the checked official corpus history did not yield the construction `commit_id` needed for immutable upstream binding.

No current-HEAD substitution, relevance-informed match, manual source substitution, or undocumented heuristic was used.

## 3. FreshStack aperture

`PASS` for the rule itself.

For each topic, the complete pinned topic corpus partition is `DETERMINISTICALLY DERIVED WITHOUT RETRIEVAL`. The pinned source loader applies the same externally specified topic subset to corpus and query loading. No candidate list is necessary.

This aperture result is subordinate to the Gate 1 falsifier: an aperture cannot make a nonexistent frozen query row valid.

## 4. SciFact reconstruction/licensing

`PASS`.

Archive SHA/size reproduced exactly. `claims_dev.jsonl` SHA-256 is `86f0435d08fdb65d1aa41d1472684f57e6e71930626497bdf4d7a9ec1a632217`; `corpus.jsonl` SHA-256 is `b8d6c89624cb2ed74dee8938effc4f5d8bd2086887880af8110d64be4ceade62`. All five frozen claims and cited abstracts resolved. Licensing is adequate for the bounded methodology with attribution obligations preserved.

## 5. Evaluator independent cross-check

`INDEPENDENCE NOT ESTABLISHED`.

The separate-agent/session surface failed before use. No evaluator B implementation was created, frozen, or compared. PR #21's same-context B implementation was not relabeled independent.

## 6. Evaluator-contract defects

`NOT ESTABLISHED`.

The contract was frozen and read, but a genuine independent implementation/cross-check was unavailable. No claim is made that the contract is sufficient or underspecified.

## 7. Independent adjudication

`NOT_EXECUTED`.

No adjudicator A or B scientific record was created because Gate 1 failed before the scientific-gold hard gate. Independent isolation was additionally unavailable.

## 8. Disagreement structure

`NOT_EXECUTED`.

No scientific judgments existed to compare.

## 9. Published-qrel comparison

`NOT_EXECUTED`.

FreshStack qrels and SciFact published evidence/rationale labels remained unexposed before termination.

## 10. Segmentation stability

`NOT_EXECUTED`.

The frozen representations were not materialized after the Gate 1 stop.

## 11. Gold invariance

`NOT_EXECUTED`.

No gold existed; no meaning-preserving or meaning-changing gold controls were run.

## 12. Project-control analysis

Only structural pre-gold facts were observed:

- FreshStack query/corpus authorship: externally authored/intended externally fixed, but two frozen selected query identities are invalid at the pinned revision;
- FreshStack source aperture: deterministically derived from topic partition;
- SciFact query/corpus/source aperture: externally fixed, with source aperture via `cited_doc_ids`;
- passage segmentation: frozen deterministic rule, not materialized;
- relevance/role/grouping/negatives: not adjudicated.

The full corpus-authoring influence audit was not executed after the hard stop.

## 13. Contamination/exposure

Terminal exposure state:

- production BM25 exposed: `false`
- Hybrid exposed: `false`
- Semantic-only exposed: `false`
- dense exposed: `false`
- lexical exposed: `false`
- FreshStack candidate-list exposed: `false`
- FreshStack published retrieval-result exposed: `false`
- historical predecessor retrieval-result exposed: `false`
- published FreshStack qrel exposed before gold freeze: `false`
- published SciFact evidence exposed before gold freeze: `false`
- scientific gold creation state: `NOT_STARTED`

## 14. Falsified alternatives

The run independently rejected the following explanations for the terminal result:

- retrieval-conditioned FreshStack aperture;
- SciFact archive non-reproducibility;
- published-qrel leakage before gold;
- acquisition-surface retrieval leakage;
- silent current-HEAD substitution in performed source work.

Judge-cue, metadata-leak, evaluator-ambiguity, segmentation, and gold-stability alternatives remain untested because the hard stop came first.

## 15. Strongest remaining alternative

A different historical FreshStack query revision may match the row counts PR #20 intended. This is the strongest explanation for how the preregistration inconsistency arose, but adopting such a revision would mutate the frozen scientific object. It therefore cannot change this terminal disposition.

## 16. Terminal disposition

# `FALSIFIED`

Promotion-critical assumption disproven: the exact frozen FreshStack query identities are reconstructable from their preregistered dataset identity.

The frozen ten-case object is not reusable as a supported Pilot 0A scientific object. Its source-only records, SciFact reconstruction evidence, aperture analysis, and procedural firewall design remain reusable as methodology evidence, but the sample itself must not be repaired in place.

No BM25, Hybrid, Semantic-only, dense, lexical, reranker, or substitute retriever is authorized. No 24–30-case apparatus is authorized.

## 17. Smallest next authorized task

Run a **source-only preregistration-integrity diagnosis**, not another completion attempt:

1. inspect only the Git history of `freshstack/queries-oct-2024-unfiltered` to determine which exact revision, if any, contains the five recorded topic row counts;
2. identify how PR #20 acquired row counts inconsistent with its pinned revision;
3. define a mandatory pre-freeze self-consistency gate that verifies dataset revision, config/file presence, physical row count, selected row existence, selected row ID/text hash, and acquisition reproducibility before a sample can be sealed;
4. require any future FreshStack object to freeze an explicit commit-bearing corpus/source identity or immutable source manifest before adjudication.

If a new pilot is justified, preregister it as a **new scientific object**. Do not amend Pilot 0A. Do not construct the 24–30-case apparatus or run any retriever in that diagnostic task.

---

## Isolation and acquisition receipts

- clean-room branch: `research-infra/external-corpus-pilot0a-mechanical-cleanroom-20260829`
- source-only Actions run 1: `33253126024`, head `2b1cbb7e00a67138384e15ce547b9be87630380a`, artifact `9714980732`, digest `sha256:f99d9cd54fdd47844d8b357a00c790233f8c6bd9254174c72db73e30b94fc51c`
- source-only Actions run 2: `33253265218`, head `ede95664a581e7958307143a800b416e0bb544e2`, artifact `9715023795`, digest `sha256:e50d7a74872de095726a194b2757097e0c6414f29b636d723fb6f6a7b29090a9`
- evaluator isolation: MainFrame/Conduit HTTP 404 before session creation; no evaluator-B implementation identity exists.

## Deviations

1. Required named CAL Pipeline governance records were not discoverable through the available GitHub code-search surface.
2. One early overbroad PR search rendered procedural PR bodies, but no prohibited retrieval value/list; the surface was then excluded.
3. Initial source-only acquisition stopped FreshStack processing at the first missing frozen query row; a second source-only fail-soft run preserved all partition counts and provenance-history status without changing the frozen object.

## What was not established

This run does not establish external-corpus passage stability, independent gold stability, evaluator independent reproduction, FreshStack immutable upstream source binding, FreshStack per-source licensing, published-qrel agreement, BM25 weakness, Hybrid superiority, Semantic-only performance, dense/lexical performance, a final benchmark, the 24–30-case apparatus, or production promotion.
