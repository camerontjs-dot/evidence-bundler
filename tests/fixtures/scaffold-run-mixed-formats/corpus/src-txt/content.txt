4.2.1 Plain Text Controls

Plain text source for loader coverage.

Plain-text intake should retain exact line breaks for downstream chunking.

Plain-text line preservation applies only when downstream chunking uses the
line-preserving loader.

The second paragraph remains a flat chunk without Markdown headings.
