# Proposition Compiler Evaluator RC1

Research-only binding-conservation successor to terminally falsified RC0.

Freeze order is enforced by repository history: target evaluator, controls, frozen RC0 baseline identity, regression surface and runners are pinned before the fresh decisive corpus/gold is added. `run_raw.py` never imports gold. `score.py` is the post-freeze scorer.

No file in this directory is a production Evidence Bundler path or Proposition Compiler implementation.
